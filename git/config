[core]
    excludesFile = ~/.config/git/ignore

[credential]
    helper = cache

[init]
    defaultBranch = main

[pull]
    ff = only

[user]
    name  = {{ name }}
    email = {{ email }}

[url "git@github.com:"]
    insteadOf = https://github.com/
